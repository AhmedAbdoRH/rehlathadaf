
'use client';
import '@/lib/firebase'; // Ensure Firebase is initialized on the server

// This is a placeholder page to ensure Firebase is initialized during the build process.
export default function AdminPage() {
  return null;
}
